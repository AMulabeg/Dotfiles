-- Shared variables
-- Migrated from the $mod / $terminal / $browser variables at the top of the
-- old hyprland.conf. Required by keybinds.lua.

return {
    mod = "SUPER",
    terminal = "kitty",
    browser = "firefox",
}
